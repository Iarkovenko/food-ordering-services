import React from 'react';
import Logo from './Logo';
import Nav from './Nav';
import UserMenu from './UserMenu';

const Header = () => (
  <header>
    <Logo />
    <Nav />
    <UserMenu />
  </header>
);

export default Header;
